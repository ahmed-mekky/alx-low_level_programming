#include <stdio.h>
#include "main.h"

/**
 * main - print file name
 *
 * @argc: number of commands
 * @argv: array of commands
 *
 * Return: Always 0.
 */

int main(int argc, char *argv[])
{
	argc = 0;
	printf("%s\n", argv[argc]);
	return (0);
}
